global:
  scrape_interval: 5s          # how often Prometheus pulls /metrics
  evaluation_interval: 5s      # how often alert/recording rules run

scrape_configs:
  - job_name: "irrigation-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8000"]  # "api" = the compose service name, resolved by Docker DNS
